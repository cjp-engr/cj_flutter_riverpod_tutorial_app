import 'package:flutter/material.dart';

class LoginRegisterWebPage extends StatelessWidget {
  const LoginRegisterWebPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
