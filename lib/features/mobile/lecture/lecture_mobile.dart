import 'package:flutter/material.dart';

class LectureMobilePage extends StatelessWidget {
  const LectureMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
