import 'package:flutter/material.dart';

class AppColors {
  int x = 0;

  static const Color textDark = Color.fromARGB(255, 255, 255, 255);
  static const Color textLight = Color.fromARGB(0, 0, 0, 0);
}
